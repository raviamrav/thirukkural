Objective:
Update the existing standalone Thirukkural flip-book app so its main reading purpose is Tamil on the left and German/English verse plus description on the right.

Output path:
C:\dev\portfolio\thirukural

Files to edit:
- C:\dev\portfolio\thirukural\generate_full_book.js
- C:\dev\portfolio\thirukural\verify_full_book.js if needed
- Regenerate C:\dev\portfolio\thirukural\index.html by running node generate_full_book.js

Current app:
- Standalone static HTML generated by generate_full_book.js.
- Data comes from thirukkural.json and detail.json.
- It currently shows Tamil couplets on the left and Tamil commentaries on the right.
- It has a chapter selector and TOC button in the top bar.

Required changes:
1. Add an EN/DE selector on the top right above the book.
   - Use a compact segmented control or two buttons labelled EN and DE.
   - Default to EN.
   - Keep it visually aligned with the existing top bar.

2. Reading spread behavior:
   - Left page must remain Tamil.
   - Right page must show the selected language, EN or DE.
   - Right page should show the corresponding verse/translation and explanation/description.
   - Use the existing data fields:
     - English verse: Translation
     - English description: explanation
     - German is not currently in the dataset; provide a clear German text fallback generated from the available English/Tamil data only if you can do it safely. If not, structure the code to support German and use English fallback text while labelling it as German unavailable. Prefer adding a small deterministic placeholder like "Deutsche Fassung folgt." over inventing translations.

3. Kural expansion:
   - Each left-side Tamil kural row should expand its description below the kural when clicked.
   - Only one kural description can be open at a time in that chapter spread.
   - Clicking an open kural should close it.
   - When a Tamil kural is opened, the matching right-side EN/DE kural must also open/show its description.
   - When clicking the right-side language kural, the matching Tamil kural must also open.
   - Closing/opening one side must keep both sides synchronized by kural number.

4. Layout:
   - Keep the book visual style: parchment pages, palm-leaf Tamil rows, restrained classic colors.
   - Avoid clipping the expanded Tamil or EN/DE description on desktop.
   - On mobile, text may scroll inside a page, but the controls should remain usable.

5. Verification:
   - Preserve checks for 133 chapters and 1330 kurals.
   - Add verification that EN/DE language control exists.
   - Add verification that clicking a kural opens exactly matching descriptions on both left and right pages.
