Patching index.html: 1) add select id chapterSelectTranslated next to TOC. 2) add CSS for .page-language-label and .leaf transitions. 3) modify tocPage to render translation when pageIndex===1. 4) modify languagePage to split translation into two .line elements and remove h2. 5) remove pointerdown early return to allow dragging from anywhere. 6) add home/end buttons and wire them. 7) add populateTranslatedSelect and call it during init. 8) update updateLeaves to show/hide pageLanguageLabel.
